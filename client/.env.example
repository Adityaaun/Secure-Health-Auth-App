# Local development can leave this empty because Vite proxies /api to localhost:4000.
# For Vercel, set this to the deployed Render API URL, for example:
# VITE_API_URL=https://your-service.onrender.com
VITE_API_URL=
